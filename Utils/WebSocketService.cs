using Microsoft.AspNetCore.Builder;

namespace BookingApi.Utils
{
    public class WebSocketService
    {
        // 保留 app 參數，對應 Program.cs 呼叫
        public void InitWebSocket(WebApplication app)
        {
            // 先空實作
        }
    }
}